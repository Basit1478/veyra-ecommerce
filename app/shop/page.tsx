import type { Metadata } from "next"; import Image from "next/image"; import { categories } from "../data"; import { ShopClient } from "./shop-client";
export const metadata:Metadata={title:"Collection"};
export default async function ShopPage({searchParams}:{searchParams:Promise<{category?:string}>}){const {category}=await searchParams;const initial=categories.includes(category as (typeof categories)[number])?category as (typeof categories)[number]:"All pieces";return <main className="shop-page"><header className="page-title collection-title"><span>THE COLLECTION / AZADI EDITION</span><h1>Objects for<br/><em>the life between.</em></h1><p>Sixteen considered forms. Each made slowly enough to become personal.</p><div className="collection-portrait"><Image src="/images/azadi-pli-mini.png" alt="Woman wearing the Pli Mini outside a bookshop" fill priority sizes="(max-width: 700px) 42vw, 22vw"/></div></header><ShopClient initialFilter={initial}/></main>}
